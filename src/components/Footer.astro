---
import { SITE_CONFIG } from '../config';

const currentYear = new Date().getFullYear();
---

<footer class="bg-gray-900 border-t border-gray-800 mt-20">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      <div>
        <h3 class="text-lg font-bold text-gray-100 mb-4">Adil Oryspayev</h3>
        <p class="text-gray-400 text-sm leading-relaxed">
          Computer Science & Mathematics student at Syracuse University. Passionate about geometric measure theory, high-performance systems, and building elegant solutions to complex problems.
        </p>
      </div>

      <div>
        <h3 class="text-lg font-bold text-gray-100 mb-4">Quick Links</h3>
        <ul class="space-y-2">
          {SITE_CONFIG.navigation.map((item) => (
            <li>
              <a href={item.href} class="text-gray-400 hover:text-su-orange text-sm transition-colors">
                {item.name}
              </a>
            </li>
          ))}
          {SITE_CONFIG.showWritingSection && (
            <li>
              <a href="/writing" class="text-gray-400 hover:text-su-orange text-sm transition-colors">Writing</a>
            </li>
          )}
        </ul>
      </div>

      <div>
        <h3 class="text-lg font-bold text-gray-100 mb-4">Connect</h3>
        <div class="space-y-3">
          <a href={`mailto:${SITE_CONFIG.email}`} class="flex items-center text-gray-400 hover:text-su-orange text-sm transition-colors">
            {SITE_CONFIG.email}
          </a>
        </div>
      </div>
    </div>
    
    <div class="mt-12 pt-8 border-t border-gray-800 flex flex-col md:flex-row justify-between items-center">
        <p class="text-gray-500 text-sm">© {currentYear} Adil Oryspayev. Built with Astro and React.</p>
        <p class="text-gray-500 text-sm mt-2 md:mt-0">GPA: <span class="text-su-orange font-semibold">3.96</span> | Syracuse University</p>
    </div>
  </div>
</footer>
